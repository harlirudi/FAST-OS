# Handoff — FacilityOS MVP

> Dibuat: 23 Agustus 2026. Lanjutkan pekerjaan dari file ini di session baru.

## BATCH 4B — FULL SYNC GOOGLE SHEETS (prioritas setelah Batch 4 — keluhan user 26/8)

**Masalah:** sheet tidak sinkron penuh dengan DB — (a) data sebelum marker (mis. 31 Juli–18 Agu) tidak pernah masuk; (b) sinkron inkremental tidak memperbarui record yang berubah (mis. override_reason diedit admin); (c) user ingin sheet = web admin (semua data, sort/filter).

**Solusi yang disepakati: setiap sinkron = REBUILD PENUH** (bukan marker-incremental):
1. `supabase/functions/sync-sheets/index.ts`:
   - Hapus pemakaian marker (atau jadikan opsional) — setiap run: reset tab Absensi & Checkpoint (`resetTab`) + tulis header + beautify + **tulis SEMUA baris** (query tanpa `.gt(marker)`, tanpa limit kecil; pasang limit 5000 untuk keamanan).
   - Paired attendance + kolom Terlambat + foto flagged — logika sudah ada, tinggal query full.
   - Marker boleh tetap di-update (tidak dipakai lagi, atau dipakai hanya untuk log).
2. Web admin Log page (`web/src/app/admin/logs/page.tsx`): tombol beri label **"Sinkron Penuh"** (tetap memanggil edge via `web/src/lib/sheets.ts` forwarder — tidak perlu ubah, cukup label).
3. Verifikasi: jalankan curl cron-secret → cek sheet berisi SEMUA event (18 absensi → ±9-10 blok; 7 checkpoint) + filter/sort berfungsi.
4. Catatan performa: rebuild penuh tiap 30 menit — aman untuk <10k baris; kalau data besar nanti, evaluasi kembali.

**Konfirmasi dari user (26/8):** sheet saat ini sudah punya filter dropdown + frozen header + conditional formatting — user mungkin melihat versi lama (perlu refresh tab browser). Sorting/filter di sheet = fitur bawaan Sheets, sama seperti web admin.

## Release APK TERBARU (batch 1-3, siap dipakai)

**Link: https://gofile.io/d/h5HSUOwk** (cert 35:BD:BB — update aman tanpa uninstall; salinan ~/Desktop/facilityos-release.apk)

**Berisi:** login Google native, role security, face matching (Rekognition, foto patokan), liveness 2 kedipan + kamera depan, foto absensi hanya flagged, kompresi checkpoint, flow istirahat 3 kondisi + GPS 5 menit, paired log, dan semua fitur sebelumnya.

## BATCH 4 — SPESIFIKASI (belum dikerjakan — session berikutnya mulai dari sini)

**Fitur: Supervisor multi-site (check-in/check-out per site + dashboard filter per site).**
Konteks pendukung: pairing sheets/web SUDAH per user+site+hari → multi-site otomatis benar; `attendance_logs.site_id` per record sudah ada.

1. **DB** — `supabase/migrations/2026xxxx_user_sites.sql`:
   - `CREATE TABLE user_sites (user_id UUID REFERENCES users(id) ON DELETE CASCADE, site_id UUID REFERENCES sites(id) ON DELETE CASCADE, PRIMARY KEY(user_id, site_id));`
   - Isi awal: `INSERT INTO user_sites SELECT id, site_id FROM users WHERE site_id IS NOT NULL;` (migrasi data lama)
   - RLS: `get_user_site_ids()` helper baru (RETURNS UUID[] — SELECT site_id FROM user_sites WHERE user_id = users.id oleh auth.uid()); supervisor policy `site_id = ANY(get_user_site_ids())`; admin full; user hanya lihat baris sendiri.
   - Policy yang diubah: users (supervisor read tim), checkpoints (supervisor), attendance_logs (supervisor), checkpoint_logs (supervisor) — ganti `site_id = get_user_site_id()` → `site_id = ANY(get_user_site_ids())`.

2. **Edge `attendance`**:
   - Izinkan role supervisor (selain cleaner/security) untuk check-in/check-out/break — body tambah `site_id` (wajib untuk supervisor; untuk cleaner/security abaikan/site sendiri).
   - Validasi: `site_id` harus ada di user_sites supervisor (cek via service role).
   - Checkpoint/scan tetap untuk cleaner/security (supervisor tidak scan checkpoint — hanya absen).

3. **Mobile**:
   - `getAttendanceStatus`: bila user punya >1 site → status per site terpilih; tambah param `siteId`.
   - CleanerHomeScreen/SupervisorDashboardScreen: site picker muncul bila multi-site (check-in: pilih site dulu; dashboard: switcher tab/dropdown di atas → tim/override/foto per site).
   - Break/GPS-out-of-area: per site yang sedang dipilih.

4. **Web admin**:
   - Halaman Pengguna: assign **banyak site** (ganti FormSelect site → multi-select, simpan ke user_sites).
   - Server action `updateUserSites(userId, siteIds[])` (transaksi: hapus + insert user_sites).
   - Dashboard & Log: filter site sudah ada (tidak berubah).

5. **Cara cek RLS**: simulasi JWT supervisor via psql (`set_config('request.jwt.claims', ...)`).

6. **Testing**: assign Dede ke 2 site → check-in site A pagi, site B siang → sheet/Log menampilkan 2 blok per site; dashboard filter per site.

## Update Sesi Ini — Batch 1-3 (commit `2066199`)

**Batch 1 — Anti-spoofing:**
- Liveness **wajib 2 kedipan** (transisi mata terbuka→tertutup), max 8 frame ±6 detik, pesan "Kedipkan mata dua kali (x/2)"; motion hanya fallback jika perangkat tanpa data mata
- **Foto absensi disimpan HANYA untuk record flagged** — mobile kirim base64 (photo_base64) → edge verifikasi via bytes → jika flagged baru upload storage; record normal tanpa foto. Offline replay baca file lokal
- Checkpoint foto kompresi **640px q0.5**

**Batch 2 — Sheets & Log:**
- `sync-sheets` = sumber tunggal (dipanggil cron 30m ATAU admin via web forwarder dengan Bearer JWT): attendance **paired** (Kerja/Istirahat, Masuk/Keluar/Durasi/Terlambat WIB/Flag/Foto), **header sekali** (deteksi format lama/tabel otomatis → reset tab = delete+addSheet), beautify (freeze, bold header, filter dropdown, CF: terlambat merah / flag kuning / istirahat abu)
- PENTING: sheetId tab DINAMIS (Checkpoint = 204582136, bukan 1); addConditionalFormatRule butuh field ranges
- Web Log page: tabel paired + badge Terlambat (hijau Tepat/merah Terlambat), kolom: Tanggal|User|Site|Jenis|Masuk|Keluar|Durasi|Terlambat|Flag/Alasan|Foto
- Test: sheet terverifikasi (header+4 baris paired+3 CF rules)

**Batch 3 — Istirahat:**
- Edge attendance: tipe `break_start`/`break_end` (tanpa validasi radius, tanpa expire sesi, is_flagged=false), pesan sesuai tipe
- Mobile: tombol 3 kondisi Check-In → Istirahat+Check-Out → Kembali; status "Beristirahat"; scan nonaktif saat istirahat; status mengandung koordinat/radius site
- **GPS out-of-area**: saat kerja & app aktif, jika >5 menit di luar radius → prompt "Catat sebagai istirahat?" / "Saya masih di sini"
- sync.ts: action offline break_start/break_end

**Sisa (belum dikerjakan):** Batch 4 — Supervisor multi-site (user_sites, check-in per site, dashboard filter per site, RLS).

## Update Sesi Sebelumnya — Face Matching (commit `1aed2ce`)

**Check-in/check-out diverifikasi wajah: selfie harus cocok dengan foto patokan.**
- **AWS**: akun standalone `harlirudi` (779714599227) — user IAM `facilityos-rekognition` + policy CompareFaces. Kredensial di Supabase secrets: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_REGION=ap-southeast-1. (Akun org lama 143568252380 diblokir SCP — JANGAN pakai lagi.)
- **DB**: `users.reference_photo_url`; threshold di `app_config` key `face_match_threshold` (default 75).
- **Edge attendance**: sebelum insert — ambil selfie + foto patokan → `awsCompareFaces` (SigV4, `_shared/aws.ts`) → tolak jika kemiripan < threshold. User tanpa foto patokan tetap lolos (rollout bertahap).
- **Mobile onboarding**: setelah nama+HP → prompt ambil foto patokan (ML Kit cek wajah → upload `reference/<userId>` → simpan).
- **Web admin Pengguna**: kolom foto patokan (thumbnail).
- **Terverifikasi**: Jenytan vs Jenytan = 100%, vs Franciscus = 0%.
- **APK release terbaru**: https://files.catbox.moe/dbb2mc.apk (cert 35:BD:BB; juga ~/Desktop/facilityos-release.apk).

## Update Sesi Sebelumnya — Google Sheets auto-sync (commit `c327ff9`)

**Log → Google Sheets auto-sync (cron 30 menit) + tombol Sync manual:**
- Edge function `sync-sheets` (Deno, cron `*/30` via pg_cron job `sync-sheets-30m` + x-cron-secret) — baca log baru sejak marker, append ke spreadsheet, update marker di `app_config` (`sheets_last_sync_attendance`/`_checkpoint`) hanya jika sukses (anti-duplikasi, retry otomatis).
- Web: server action `syncToSheets` (tombol "Sync ke Google Sheets" di halaman Log) — logika identik, format kolom sama: **Waktu Sinkron | Waktu Log | User | Site | ...**.
- Secrets service account di-set di Supabase (`supabase secrets set` — CLI Supabase AMAN, tidak seperti vercel CLI): GOOGLE_SHEETS_CLIENT_EMAIL / PRIVATE_KEY_B64 (base64 PEM) / SPREADSHEET_ID.
- Marker baseline diinisialisasi (log lama TIDAK di-backfill — arsip dimulai dari sekarang; kalau mau backfill, set marker ke tanggal lama di app_config).
- Verifikasi: fungsi 200 (0 baris, marker baru), tanpa secret 401, tombol web OK, kedua cron job aktif.

## Update Sesi Sebelumnya — Google Sheets export manual (commit `2d5b45f`)

**Export ke Google Sheets berfungsi end-to-end di production** (terverifikasi: 2 baris tertulis ke tab "Absensi").
- Setup: service account `facilityos-sheets@fast-os-505916.iam.gserviceaccount.com` (Google Cloud project fast-os-505916, Sheets API enabled), spreadsheet "FacilityOS Log" di-share Editor ke service account, spreadsheet ID `1Z4qKXhjkQzO3a9A_EZNSP_qzjX6_Hp22jXRyD2DW6Hc`.
- Env Vercel (SET VIA DASHBOARD — CLI terbukti merusak nilai): `GOOGLE_SHEETS_CLIENT_EMAIL`, `GOOGLE_SHEETS_PRIVATE_KEY_B64` (base64 PEM, BUKAN JSON), `GOOGLE_SHEETS_SPREADSHEET_ID`. Juga di `.env.local`.
- **PELAJARAN PENTING:** `vercel env add` via CLI memotong/mengacaukan nilai (hanya ±28 karakter tersimpan); selalu set env via dashboard Vercel. Kesalahan umum lain: nama key ikut ter-paste ke dalam Value, dan nilai tertukar antar variabel.
- Tombol "Export ke Google Sheets" di halaman Log (tab Absensi/Checkpoint) — append dengan kolom "Diekspor", tanpa auto-download.

**Catatan deploy:** commit kosong (`git commit --allow-empty`) dipakai untuk trigger deploy setelah env berubah (Vercel tidak auto-redeploy hanya karena env change dari dashboard kadang). Pastikan `vercel ls` menunjukkan deploy terbaru Ready.

**Lainnya sesi ini:** role Security selesai (check constraint users_role_check diperbaiki), dashboard spotlight keterlambatan (sites.start_time, WIB), halaman Log dengan filter/pagination, retensi 30 hari (cron pg_cron + CRON_SECRET), geocoding Google API jalan.

## Update Sesi Sebelumnya (commit `de16696`)

**1. Fix tombol check-in terkunci loading:** `handleAttendance`/`handleOverride` — `return` awal melewati `setActionLoading(false)`. Fix: `try/finally`. Semua jalan pembatalan (foto tanpa wajah, batal, upload gagal) kini tombol langsung aktif kembali.

**2. Setup build lokal (kuota EAS habis, reset 1 Sep):**
- Prasyarat: `brew install openjdk@17` (JBR Android Studio = Java 25 terlalu baru utk gradle RN); NDK 27.1 auto-install oleh gradle.
- Release: `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ANDROID_HOME=~/Library/Android/sdk npx eas-cli build --local -p android --profile preview --non-interactive` → `mobile/build-*.apk` (gitignored; 133MB semua ABI; cert 35:BD:BB).
- Dev client: profile `development` + `credentialsSource: local` (komit) → `eas build --local -p android --profile development` → install bisa menimpa release (signing sama).
- Metro: `npx expo start --dev-client` (background, log /tmp/metro.log). Dev launcher: Enter URL manual `exp://<IP-Mac>:8081` (hati-hati autocorrect ".net" — clear field dulu).

**3. Status HP Jeny (vivo V2130):** dev client terpasang & terhubung Metro (hot reload aktif). Session Jenytan (PhiKitchen) tersimpan — langsung CleanerHomeScreen. **Menunggu verifikasi user:** tombol check-out tidak terkunci setelah peringatan foto non-selfie.

**Catatan urutan untuk iterasi berikutnya:** ubah kode → Metro hot reload → test di HP → build release APK lokal hanya saat mau distribusi.

## Update Sesi Sebelumnya (commit `4aa0f5e`)

**1. Delete user web admin** — terverifikasi e2e (konfirmasi → hapus → tabel refresh tanpa reload). Jenytan masih ada karena hapus pertama terjadi sebelum fix; bisa dihapus via UI.

**2. Dropdown site tidak persist — ROOT CAUSE:** trigger `prevent_privilege_change` yang saya buat memanggil `get_user_role()` tanpa prefix `public.` sementara trigger punya `SET search_path=''` → fungsi tidak ter-resolve → **semua UPDATE users dari web admin error diam-diam**. Fix: `public.get_user_role()` di trigger. Terverifikasi e2e (ubah site → persist + tabel refresh).

**3. Face detection menolak selfie — ROOT CAUSE:** hasil `detectFaces` di Android TIDAK punya field `success` (hanya `faces`+`imagePath`); cek `!result.success` selalu true → semua foto ditolak. Fix: cek `faces.length`; tambah validasi posisi (wajah ≥15% ukuran gambar, tengah ±40%) untuk selfie konsisten. APK baru terpasang di HP — **menunggu test manual selfie**.

**4. Jarak karyawan 10-15m dari PhiKitchen gagal:** radius sudah 50m — jarak segitu harus lolos → kemungkinan **koordinat site tidak tepat**. Saran: update koordinat via app supervisor "Lokasi Site" dari dalam gedung; atau naikkan radius; jangka panjang NFC tap. **Menunggu keputusan user.**

**5. Kuota EAS build habis (reset 1 Sep)** → **build lokal** berhasil: `JAVA_HOME=/opt/homebrew/opt/openjdk@17/... npx eas-cli build --local -p android --profile preview --non-interactive` (JBR Android Studio = Java 25 terlalu baru; openjdk@17 via brew; NDK auto-install). APK: `mobile/build-*.apk` (gitignored). Sebelumnya EAS cloud: 91MB; lokal 133MB (semua ABI). Keystore lokal 35:BD:BB konsisten.

**Status:** semua fix terverifikasi kecuali face detection selfie (butuh test manual user).

## Update Sesi Sebelumnya (commit `eae7658` — batch fix)

**1. RLS role akun Google (bug serius):** `get_user_role()` membaca `auth.jwt() -> user_metadata.role` — KOSONG untuk akun Google → cleaner Google tidak bisa membaca attendance_logs sendiri → check-in tercatat (edge function) tapi app selalu "Belum Check-in". Fix: baca dari tabel `users` (SECURITY DEFINER, `public.users`); policy users/sop_alerts + trigger prevent_privilege_change ikut dirapikan. Migrasi `20260823230000_fix_get_user_role_google.sql` sudah di-apply ke cloud & terverifikasi (simulasi JWT Google user → role terbaca). **TERVERIFIKASI di HP: status "Sudah Check-in" kini benar.**

**2. Greeting nama:** "Halo, {nama}!" — nama dari tabel users via AuthContext (state `name`).

**3. Anti-spoofing selfie (Level 1):** ML Kit face detection — `@infinitered/react-native-mlkit-face-detection@4.0.0` + `expo-image@~3.0.11` (v5 menarik expo-image@57 → crash `NoClassDefFoundError AnyTypeCache` — WAJIB versi 4!). Flow check-in: foto → kompres → deteksi wajah (tolak tanpa wajah: "Harap gunakan foto selfie Anda") → upload (gagal = batalkan, tidak ada lagi fallback placeholder) → GPS → submit. FaceDetectionProvider di App.tsx.

**4. Web admin delete:** langsung refresh tabel + konfirmasi (sebelumnya harus reload browser).

**APK terbaru (terverifikasi di HP Jeny):** build `25b20e1f` → https://expo.dev/artifacts/eas/WaRO_2PQvqw-Wzdl9xIiTA-a3DpocGa7tPM4jiD4Dg4.apk — login native OK, greeting OK, status check-in OK, ML Kit termuat.

**Menunggu verifikasi user:** tes face detection manual (selfie lolos, foto bukan wajah ditolak) + delete user di web (hard refresh sekali).

## Update Sesi Sebelumnya (NATIVE GOOGLE SIGN-IN ✅ — commit `f93d9a3`)

**MASALAH LOGIN GOOGLE DI ANDROID SOLVED — native sign-in terverifikasi end-to-end:**
- Gejala lama: OAuth browser (custom scheme `facilityos://` maupun App Links https) gagal — Chrome memblokir redirect custom scheme; verifikasi App Links tidak andal di ROM vivo (verifier gagal instan, state 1024).
- **Solusi final:** `@react-native-google-signin/google-signin` v16 + `GoogleSignin.configure({ webClientId })` + `supabase.auth.signInWithIdToken({ provider: "google", token })`. Tanpa browser. 
- **Google Cloud Console (sudah dibuat user):** OAuth client tipe ANDROID baru — package `com.harlirudi.facilityos`, SHA-1 `F4:80:09:BA:0F:3A:C2:F5:7F:1D:E0:5C:35:8A:F4:C2:F3:A9:55:96` (project `fast-os-505916`). Client WEB lama JANGAN dihapus (dipakai Supabase + login web). Test users sudah terisi.
- **TERVERIFIKASI di vivo V2130 (HP Jeny):** ketuk login → bottom sheet Google → session dibuat 23:31:20 UTC (jenytan.galaxytab@gmail.com, last_sign_in_at di auth.users) → langsung CleanerHomeScreen "Halo, Cleaner! PhiKitchen". APK: build `27507a1c` → https://expo.dev/artifacts/eas/2h1tyZbXinSAJe4ojvhq5WaOHBZUpTYF27fvA6_EuYM.apk
- **Keystore:** local (RSA, `35:BD:BB`) — terpakai konsisten sejak credentialsSource local. `scripts/apk-signing-cert.sh` (apksigner via JBR Android Studio) untuk verifikasi; cert tiap build wajib dicek (assetlinks masih mengarah 35:BD:BB; jika keystore diganti, update assetlinks + user wajib uninstall).

**Catatan:**
- Debug keystore tidak ada di mesin — dev client/Metro sign-in native akan DEVELOPER_ERROR sampai ada OAuth client Android dengan SHA-1 dev/EAS keystore dev. Untuk dev, gunakan akun admin web atau rebuild dev client.
- `Linking` cold-start exchange tetap ada di AuthContext (defensif, tidak dipakai flow native).

## Update Sesi Sebelumnya (App Links — commit `e97b1cd`)

**Masalah:** login Google di APK standalone tidak pernah kembali ke app (browser diblokir Chrome saat redirect ke custom scheme `facilityos://`; `openAuthSessionAsync` maupun `Linking` custom scheme sama-sama gagal di perangkat user).

**Solusi: Android App Links.**
- Redirect OAuth app → `https://web-chi-cyan-20.vercel.app/auth/callback` (https normal, tidak diblokir)
- Android intercept URL itu ke app karena: (1) `app.json` android.intentFilters autoVerify https://web-chi-cyan-20.vercel.app/auth/callback; (2) `web/public/.well-known/assetlinks.json` mendeklarasikan package `com.harlirudi.facilityos` + SHA-256 cert.
- AuthContext: standalone pakai redirect https, dev pakai `facilityos://` (__DEV__), timeout 60 detik, recovery session + exchange code saat cold start.

**PENTING — keystore EAS:**
- Fingerprint cert APK aktual: `66:69:20:D6:71:3F:CD:87:D6:EA:BF:27:61:9D:C6:8A:74:48:E1:1D:2B:BF:25:25:F1:BD:0D:3D:81:F4:76:D4` (EC, cert 264 byte).
- Keystore lokal (RSA, `mobile/facilityos-release.keystore`, 35BDBB8B) TIDAK terpakai oleh server build meski CLI bilang "Using local Android credentials" — akar masalah belum ketemu (dugaan: server pakai keystore ter-cache "Build Credentials W_ItbpQ31n").
- Build 1–3 (era remote credentials) cert BERBEDA tiap build (bug rotasi); sejak `credentialsSource: local` (build 4+) stabil di 666920D6.
- **WAJIB setiap build baru:** jalankan `python3 scripts/apk-signing-cert.py <apk>` dan cocokkan dengan assetlinks.json. Kalau berubah → update assetlinks.json + user wajib uninstall app lama.
- `credentials.json` (password keystore lokal) tetap gitignored; keystore ter-track git (EAS arsip build dari file ter-track).

**APK distribusi saat ini (sudah App Links):** build `a931c97c` → https://expo.dev/artifacts/eas/37a0khT9TbhwevrG1hGjb32L_8baRGj7rvJ26azVP50.apk

**Cara test:** uninstall app lama dulu (signature beda) → install APK → tap Masuk/Daftar dengan Google → pilih akun → browser tertutup otomatis & kembali ke app. Catatan: verifikasi App Links terjadi saat install (perlu internet).

## Update Sesi Sebelumnya (commit `be172ef`) — login Google deep link
- Gejala: tap Google di APK → browser terbuka → tidak kembali ke app → "Gagal login Google". Browser malah menampilkan sesi web lama ("Selamat datang...").
- Pengecekan: manifest APK SUDAH punya intent-filter `facilityos://` (VIEW+BROWSABLE) di MainActivity; `makeRedirectUri` menghasilkan `facilityos://auth/callback` dengan benar. DB/RLS aman.
- Akar masalah: `WebBrowser.openAuthSessionAsync` gagal hand-off hasil redirect di build standalone Android (Chrome Custom Tab) — dev client jalan, standalone tidak.
- Fix (`be172ef`): ganti dengan deep link `Linking` — `Linking.createURL("auth/callback")` + `openURL` + listener `url` + `getInitialURL` (cold start) + timeout 180 detik + fallback recovery session. Juga exchange kode saat cold start di useEffect awal.
- APK baru: build EAS `60d9e0d7-...` (preview, buildType apk).

**Konfigurasi:** `eas.json` preview → `android.buildType: "apk"` (commit `d07a139`). Build APK: `npx eas-cli build -p android --profile preview --non-interactive --no-wait`.

**Penjelasan web vs mobile (sudah disampaikan ke user):** satu akun Supabase, dua pintu: web = daftar + admin + halaman info status; app mobile = aplikasi kerja. Login terpisah per perangkat, data (nama/HP/role/site) dibagikan.

## Update Sesi Sebelumnya (commit `45e7cf8`) — Flow A polling penugasan

1. Mobile: `WaitingAssignmentScreen` polling `refreshProfile()` tiap 30 detik + tombol "Periksa Sekarang" + "Terakhir diperiksa HH:MM" + refresh otomatis saat app ke foreground (AppState di AuthContext). Begitu site di-assign → otomatis masuk CleanerHomeScreen/SupervisorDashboard (tanpa restart).
1. Mobile: `WaitingAssignmentScreen` polling `refreshProfile()` tiap 30 detik + tombol "Periksa Sekarang" + "Terakhir diperiksa HH:MM" + refresh otomatis saat app ke foreground (AppState di AuthContext). Begitu site di-assign → otomatis masuk CleanerHomeScreen/SupervisorDashboard (tanpa restart).
2. Web dashboard: auto-refresh (`router.refresh()`) tiap 30 detik saat user belum punya site + catatan "Halaman ini diperbarui otomatis setiap 30 detik."
3. Web admin: toast "Penugasan tersimpan — user akan otomatis masuk aplikasi dalam ≤30 detik." (`<Toaster />` di admin/layout, `toast.add` di users page).
4. Semua terverifikasi e2e (toast muncul, dialog menutup, dashboard waiting + auto-refresh jalan).

**Data DB (ditinggalkan user saat testing, jangan diubah):** Dede Setiawan = supervisor tanpa site, CuteNenk = cleaner PhiKitchen, Cleaner/Supervisor = Gedung Utama, Jenytan & Franciscus = PhiKitchen.

**Menunggu verifikasi user (mobile):** polling 30s + tombol Periksa Sekarang (cukup reload JS dari Metro, tidak perlu dev build baru). Test: buka layar menunggu → admin assign site → dalam ≤30 detik otomatis masuk layar kerja.

## Update Sesi Sebelumnya (commit `fd31bca`, sudah di-push & auto-deploy)

1. **Dialog edit pengguna (web admin)**: sudah di-verify via e2e (lokal + production) bahwa dialog menutup setelah simpan — fix sebelumnya (`62941be`) terbukti jalan. Ditambah: pesan error tampil di dialog kalau update gagal (tidak lagi gagal senyap).
2. **Login Google mobile**: `signInWithGoogle` sekarang recovery session — kalau browser tertutup tanpa kode OAuth tapi session sudah terbentuk di server, langsung lanjut (tidak muncul layar login dua kali). Tombol jadi "Masuk / Daftar dengan Google" (mobile + web).
3. **Status "Menunggu Penugasan"**: web dashboard kini tampilkan teks besar (3xl, amber) saat user belum punya site. Mobile WaitingAssignmentScreen: judul diperbesar ke 28pt.
4. Data test sudah dikembalikan: Dede Setiawan = cleaner tanpa site (untuk test status menunggu), Jenytan & Franciscus = PhiKitchen, Cleaner/Supervisor = Gedung Utama.

**Menunggu verifikasi user:**
- Test login Google di Android (dev build + Metro): login → onboarding → "Menunggu Penugasan" (28pt) → admin assign → CleanerHomeScreen
- Web dashboard Dede Setiawan (login Google) → harus tampil "Menunggu Penugasan" besar
- Dialog edit pengguna di production (hard refresh dulu)

## Status Ringkas

FacilityOS MVP hampir selesai. Fitur inti (absensi GPS+selfie, patroli checkpoint NFC/QR, SOP monitoring, dashboard web+supervisor) sudah jalan di production. Saat ini sedang merampungkan **login Google untuk semua role** + onboarding data diri.

## Infrastruktur

### Supabase Cloud
- URL: `https://vbzbyxmcpwppvfpbxsls.supabase.co`
- Project ref: `vbzbyxmcpwppvfpbxsls`
- Helper query DB: `./scripts/psql.sh "SQL..."` (berisi credential, sudah gitignored)

### Vercel (Web)
- Production: `https://web-chi-cyan-20.vercel.app`
- Root directory sudah di-set ke `web` (auto-deploy dari GitHub `main` sekarang BERFUNGSI)
- Setiap push → auto-deploy (~1 menit)

### Google OAuth
- Sudah aktif di Supabase (via Management API)
- Client ID: `424874598334-93696k6ja8cec2gpkngp6meo31nmmoti.apps.googleusercontent.com`
- Test users ditambahkan di Google Cloud Console → OAuth consent screen (status Testing, max 100 user)

### Mobile (Expo SDK 54, dev client)
- Dev build harus diinstall ulang (terbaru termasuk expo-auth-session, expo-web-browser, scheme `facilityos`)
- Metro: `cd mobile && npx expo start --dev-client`
- IP Mac berubah-ubah saat ganti WiFi — update URL di app: menu → "Enter URL manually"

## Akun Test (dipertahankan untuk demo)

| Akun | Password | Role |
|------|----------|------|
| `admin@test.id` | password123 | admin (login via `/login-admin`) |
| `spv@test.id` | password123 | supervisor |
| `cleaner@test.id` | password123 | cleaner |
| `jenytan.galaxytab@gmail.com` | (Google) | cleaner, site PhiKitchen |

## Data Saat Ini
- Sites: Gedung Utama, Gedung Annex, PhiKitchen (`d8a463d3-...`)
- Jenytan sudah di-assign ke PhiKitchen

## Fitur Baru (Sesi Ini — Login Google)

**Sudah selesai & ter-deploy:**
1. Web `/login` = tombol Google saja; `/login-admin` = email/password admin (tidak di-link)
2. Web `/onboarding` = data diri (nama dari Google + HP wajib)
3. Web `/auth/callback` = tukar kode OAuth
4. Middleware redirect user tanpa HP → onboarding
5. Mobile: LoginScreen (Google), OnboardingScreen, WaitingAssignmentScreen
6. RLS: user bisa UPDATE/INSERT profil sendiri + trigger anti-escalation (non-admin tidak bisa ubah role/site)
7. FormSelect fix: Base UI Select butuh prop `items` (label tampil, bukan UUID) ✅ user sudah konfirmasi nama site tampil
8. Dialog edit pengguna menutup otomatis (updateUserRole tanpa redirect, client setOpen(false)) ✅ baru deploy

**Menunggu verifikasi user:**
- Dialog menutup otomatis (baru deploy, perlu hard refresh)
- Login Google di mobile (dev build baru + Metro connect)

## Hal yang Belum Selesai / Diskusi Berjalan

1. **Test login Google end-to-end di Android** — install dev build terbaru → connect Metro → login Google (1x) → onboarding → "Menunggu Penugasan" → admin assign → CleanerHomeScreen
2. **Anti-spoofing selfie check-in** (diskusi):
   - Level 1: deteksi wajah (Google ML Kit Face Detection, gratis) — rekomendasi MVP
   - Level 2: face matching vs foto referensi (API berbayar) — post-MVP
   - Level 3: active liveness (kedip/senyum) — post-MVP
   - Alternatif: NFC tap di pintu untuk check-in (infrastruktur sudah ada)
   - User belum memutuskan
3. **Trial 30 orang** — butuh build preview APK standalone (tidak perlu Metro) untuk dibagikan
4. **3 akun test** — dihapus nanti setelah semua settle

## Konvensi
- Commit message: Bahasa Indonesia, conventional commits (feat/fix/chore/test)
- Typecheck sebelum commit: `npx tsc --noEmit` di folder masing-masing
- Test: `npm test` (web 17, mobile 45)
- Deploy web: otomatis via push ke main
- Edge Functions deploy: `supabase functions deploy <nama>` (token akses ada di session baru, jangan commit)
- Supabase access token: lihat catatan lokal / mintakan ke user (JANGAN commit ke repo)

## Tech Debt Tersisa (post-MVP)
- NFC pairing via supervisor mobile (sebagian sudah ada: NfcPairingScreen)
- In-app push notification SOP cleaner
- Face matching/liveness untuk selfie
- Hapus 3 akun test setelah settle
